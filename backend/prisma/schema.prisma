generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Task {
  id   Int     @id @default(autoincrement())
  text String
  done Boolean @default(false)
}
